import { List, styled } from '@mui/material';

export const NavList = styled(List)(() => ({
  display: 'flex',
  flexDirection: 'column',
  gap: '10px',
  paddingTop: '15px',
  paddingBottom: 0,
}));
